import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add proper smoke test once app is stable.
    expect(1 + 1, equals(2));
  });
}
